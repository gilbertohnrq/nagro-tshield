import 'package:flutter/material.dart';

class CreditRequestFinishPage extends StatelessWidget {
  const CreditRequestFinishPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
