import 'package:flutter/material.dart';

class BiometricsProposalConditionsPage extends StatelessWidget {
  const BiometricsProposalConditionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
