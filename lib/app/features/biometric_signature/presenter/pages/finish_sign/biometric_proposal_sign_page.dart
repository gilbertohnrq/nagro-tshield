import 'package:flutter/material.dart';

class BiometricProposalSignPage extends StatelessWidget {
  const BiometricProposalSignPage({super.key});

  static const route = 'proposal/sign';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
